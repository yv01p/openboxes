<?xml version="1.1" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                       https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd"
                   logicalFilePath="services/identity-service/db/changelog/changelog-create-table-user-role.xml">
    <changeSet id="phase2-shadow-create-user-role" author="openboxes-identity">
        <preConditions onFail="MARK_RAN" onFailMessage="user_role table not found — Grails Liquibase must run first to create table per grails-app/migrations/install/changelog-create-tables.groovy">
            <columnExists tableName="user_role" columnName="user_id"/>
        </preConditions>
        <comment>
            Shadow for user_role join table. Grails Liquibase owns table creation via install/changelog-create-tables.groovy.
            identity-service uses spring.jpa.hibernate.ddl-auto=validate to prove entity-mapping correctness.
            This changeset marks-ran if Grails already created the table; no DDL body needed.
        </comment>
        <!-- No body: table already exists per the precondition. -->
    </changeSet>
</databaseChangeLog>
