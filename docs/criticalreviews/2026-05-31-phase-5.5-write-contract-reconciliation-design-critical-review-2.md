# Critical Design Review: 2026-05-31-phase-5.5-write-contract-reconciliation-design (Round 2)

**Spec:** `/home/yv01p/openboxes/docs/specs/2026-05-31-phase-5.5-write-contract-reconciliation-design.md`
**Verified Assumptions section:** present

Round 2 focuses on the surface changed since round 1: the FD-1 resolution in §3.2 (per-task-extensible advice set; T3 throws a custom exception mapped to 409) and the new A9. Round-1-cleared items (A1–A8, §2 empty) are not re-litigated.

## 1. Verified-assumptions cross-check

- **A1–A8** — reconfirmed. Cited evidence (`useProductSupplierForm.js`, `SecurityConfig.java`, `ProductSupplierController.java`, `useProductSupplierListTableData.jsx`, `useTableData.jsx`, identity `GlobalExceptionHandler.java`, `app.conf`) is unchanged since round 1's confirmation; no commits touched those files (only the spec changed at `7d874e232`).
- **A9** (new) — reconfirmed, rigorously. `SHOW INDEX FROM product_supplier_preference` shows the only `Non_unique=0` index is `PRIMARY(id)` (single-column); the `product_supplier_id` and `destination_party_id` indexes are both `Non_unique=1`, single-column, and no composite index over the pair exists. T3 pair-uniqueness is genuinely app-layer-only, exactly as A9 states.

## 2. Literal-wrongness findings

No literal-wrongness findings.

The FD-1 fix surface was examined for new gaps and is coherent:
- The advice **class** with its base mappings (`DataIntegrityViolationException → 409`, bad-input → 400) lands as correction #2 before T3; T3 **extends** it with the `DuplicatePreferenceException → 409` handler when the exception type itself is introduced. No ordering contradiction — the class exists before T3 extends it.
- T3 is backend + integration-tests only (§4), so its pair-uniqueness rejection is exercised by integration tests calling catalog-service directly. The 401-mask (`/error` forward → `HttpStatusEntryPoint(UNAUTHORIZED)`) reproduces in a full-filter-chain `@SpringBootTest`, so the fix is genuinely needed there and the `→ 409` handler delivers the asserted outcome. The post-revert routing change (correction #1 sends `/api/productSuppliers` back to Grails) does not undercut this, because the integration tests bypass nginx.

## 3. Forced decisions

No forced decisions found.

(The "advice handled-exception set extended per task" framing was checked for a hidden unpicked choice for T4/T5: ProductPackage's uniqueness is DB-enforced — verified gotcha — so it surfaces as `DataIntegrityViolationException → 409` via the existing base mapping, needing no custom exception; ProductPrice has no uniqueness rule in scope. So no new per-task exception decision is forced.)

## 4. Previously addressed

- **FD-1** (round 1 forced decision — the §3.2 advice taxonomy didn't cover T3's app-layer pair-uniqueness rejection) is **resolved**. §3.2 now specifies the per-task-extensible advice set and the custom-exception → 409 path; A9 grounds the no-pair-unique-index fact the resolution rests on.

## 5. Recommendation

✅ **Approve as-is** — §2 and §3 are both empty. The spec is ready for implementation planning (`update-implementation-plan` to re-sequence T3–T5 backend-only, add the list-query sub-task, and add the cluster-end cutover task).
