# Critical Design Review: 2026-05-29-phase-4.1-cleanup-design (Round 2)

**Spec:** `/home/yv01p/openboxes/docs/specs/2026-05-29-phase-4.1-cleanup-design.md`
**Verified Assumptions section:** present

## 1. Verified-assumptions cross-check

Fresh read of each cited evidence reference, post-R1 fix (spec HEAD = `17c112b36`). The codebase + running stack are unchanged since R1 (minutes apart). Counts: 24 of 24 reconfirmed.

- **A1-A4** (new files/dirs absent): still absent.
- **A5-A6** (4 e2e specs + 4 Java/test/nginx targets exist): still present. NOTE: A6's wording "Java/test/nginx files for 4.1-T5" became stale after R1 (the R1 fix moved RC-10 nginx-comment out of T5 into T4); the 4 files for T5 are now `PartyRole.java`, `PartyService.java`, `OrganizationServiceIntegrationTest.java`, `organization-service.spec.ts` — none nginx. The assumption (4 files exist) still holds; only the categorical descriptor is stale. Not a §1 failure (assumption still grounded in evidence); flagged here for transparency.
- **A7** (3-migrate + 1-breadcrumb scope): reconfirmed.
- **A8-A11** (signatures): reconfirmed via re-read of files in context.
- **A12-A15** (commands): reconfirmed.
- **A16-A18** (task ordering): reconfirmed.
- **A19** (RC-1 grounding via `8cba628f1`): reconfirmed.
- **A20** (Claude Code CLAUDE.md auto-read): reconfirmed.
- **A21-A22** (consumer impact): reconfirmed.
- **A23** (commit convention `phase 4 task N:` extended to `phase 4.1 task N:`): reconfirmed. NOTE: the recent `update-design-doc`-skill-generated commit (`applied 1 fix from ...`) does NOT follow this convention — but that's a skill-managed meta-commit, not a Phase 4.1 task commit. A23's claim about TASK commits stands.
- **A24** (8 nginx blocks share triplet, added R1): reconfirmed via re-read of grep output from R1 (lines 11-66 of app.conf, 8 blocks).

All 24 verified assumptions reconfirmed under fresh evidence read.

## 2. Literal-wrongness findings

No literal-wrongness findings.

## 3. Forced decisions

No forced decisions found.

## 4. Previously addressed

From R1 review history:
- **R1 §3 Decision 1** (nginx proxy_params migration scope, 6 vs 8 vs 6+breadcrumb): resolved. User picked option (b) — 8 blocks, full triplet coverage. Spec revised at SHA `17c112b36` with 4 edits: Architecture §3 + Scope table 4.1-T4 + Done state §6 all reflect 8-block migration; new verified assumption A24 grounds the 8-block scope in R1 §3's grep evidence (8 blocks enumerated at app.conf lines 11-66).

## 5. Recommendation

✅ **Approve as-is**

§2 and §3 both empty. All 24 verified assumptions reconfirmed under fresh evidence. The R1 fix (nginx scope expansion) was applied cleanly and the new A24 grounds the expanded scope explicitly. Spec is ready for implementation planning via `thorough-writing-plans`.
