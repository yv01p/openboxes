# Critical Implementation Review: 2026-06-01-phase-5.5-write-contract-reconciliation-implementation-plan (Round 3)

**Plan:** `/home/yv01p/openboxes/docs/plans/2026-06-01-phase-5.5-write-contract-reconciliation-implementation-plan.md`
**Verified plan-level assumptions section:** present

⚠️ 3 commits since plan-write time (SHA `7d874e232`); cited file:line references re-checked under §1. (All three — `bdc167a1d`, `629e2e855`, `1f312dbe3` — are the plan's own creation + the round-1/round-2 fix applications; no cited codebase evidence changed.)

## 1. Verified-plan-assumptions cross-check

The round-2 UIP edits touched only LQ Step 1 (fetch mechanism) and T5's verb-scope bullet — not the `Verified plan-level assumptions` table (PV1–PV25) or any codebase file. Fresh reads this round: PV17 (form save fan-out + payload builders), and the Grails preference batch service/controller cited below.

- PV1–PV25 — **all still hold** (no assumption failed under fresh read). PV17 re-confirmed against `useProductSupplierForm.js` `buildPreferencesPayload:222-262` / `buildPackagePayload:265-277` and `ProductSupplierPreferenceService.groovy:16` (`saveOrUpdateBatch` is a pure per-item upsert — consistent with the plan's T3 batch semantics).

No assumption failed.

## 2. Literal-wrongness findings

No literal-wrongness findings.

(Probed this round and cleared: whether the Grails preference `/batch` uses delete-missing/replace semantics that the plan's upsert-only T3 would silently break — `ProductSupplierPreferenceService.saveOrUpdateBatch` (`grails-app/services/org/pih/warehouse/product/ProductSupplierPreferenceService.groovy:16-23`) iterates and `save()`s each item with no delete-of-removed, so the plan's "upsert each" matches Grails exactly; no gap. The round-2 fixes were also re-examined for introduced breakage — `@EntityGraph(attributePaths="product")` + `EXISTS` + `Pageable` is count-safe, and the package-POST price-persistence is internally consistent with the details→package fan-out order — neither introduced a new fault.)

## 3. Forced decisions

No forced decisions found.

## 4. Previously addressed

Round-2 findings resolved by the current plan state:

- **§2.1 (LQ fetch-join count-query trap)** — LQ Step 1 now fetches `product` via `@EntityGraph(attributePaths = "product")` (count-safe with `Pageable`) instead of a JPQL `JOIN FETCH`; explicit `countQuery` named as the fallback if a custom `@Query` is used.
- **§2.2 (package POST is an unimplemented ProductPrice write path)** — T5's verb-scope bullet now specifies the `ProductPackage` POST persists the embedded prices (`ProductPackage.productPrice` + the supplier contract `ProductPrice`, linking `ProductSupplier.contractPrice`) from `contractPricePrice`/`contractPriceValidUntil` + package price, with an integration test; the "no React write caller / read-only at cutover" wording is corrected.

All six round-1 findings (§3.1 getProduct, §2.1 LQ join-multiplication, §2.2 list-table breakage, §2.3 DTO mapping location, §2.4 pair-uniqueness holes, §2.5 live-schema mapping proof) remain resolved (confirmed in Round 2 §4; unchanged since).

## 5. Recommendation

✅ **Approve as-is** — §1 has no failed assumptions; §2 and §3 are both empty. The two prior rounds' findings are resolved and the fixes introduced no new faults. Plan is ready for `subagent-driven-development`.
