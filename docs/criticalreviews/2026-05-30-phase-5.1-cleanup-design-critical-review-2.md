# Critical Design Review: 2026-05-30-phase-5.1-cleanup-design (Round 2)

**Spec:** `/home/yv01p/openboxes/docs/specs/2026-05-30-phase-5.1-cleanup-design.md`
**Verified Assumptions section:** present

## 1. Verified-assumptions cross-check

All 18 verified assumptions reconfirmed. No codebase changes since CDR-1 round; no spec edits invalidated any cited evidence. A1-A18 all still hold per the same evidence cited in CDR-1 §1.

## 2. Literal-wrongness findings

No literal-wrongness findings.

## 3. Forced decisions

No forced decisions found.

## 4. Previously addressed

All 5 findings from CDR-1 are resolved by the current spec state (commit `4e7a4eee1`):

- **CDR-1 §2.1** (T1 build.gradle bootJar) → §4.1 "Module layout" build.gradle bullet now includes `bootJar { enabled = false }` + `jar { enabled = true }` with rationale about the inherited Spring Boot plugin.
- **CDR-1 §2.2** (T3 MODIFY enumeration) → §5 T3 row updated: action description acknowledges all 3 in-service consumers (`AuthService.java:69,:101`, `CookieService.java:10`, `IdentityServiceIntegrationTest.java:404`); MODIFY count 4 → 6 with `AuthService.java` and `IdentityServiceIntegrationTest.java` added.
- **CDR-1 §2.3** (file-count inconsistency 9 vs 8) → §4.1 "Net file delta" now reads "8 file deletions + 1 rename" with explicit breakdown; T2 DELETE column "6 (filter + JwtService × 3 canonical services...)"; done state #8 says "8 deleted local JWT files confirmed".
- **CDR-1 §2.4** (done state #8 find pattern) → §6 done state #8 rewritten with `| grep -v 'services/jwt-auth-common/'` exclusion + separate `find services/identity-service -name 'JwtIssuerService.java'` confirmation for the rename.
- **CDR-1 §3.1** (T8/T9 ordering vs React peer-dep) → §4.3 + §5 swapped: T8 is now React peer-dep (lands FIRST), T9 is node engine bump (lands AFTER T8 with explicit pre-condition note); "Why this task split" rationale and §7 risks Gradle-node-plugin paragraph both reference T9 now. Resolves the forced decision per user pick (option A).

## 5. Recommendation

✅ **Approve as-is** — §2 and §3 are both empty. Spec is ready for implementation planning.
