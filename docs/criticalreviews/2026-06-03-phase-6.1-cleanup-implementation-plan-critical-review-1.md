# Critical Implementation Review: 2026-06-03-phase-6.1-cleanup-implementation-plan (Round 1)

**Plan:** `/home/yv01p/openboxes/docs/plans/2026-06-03-phase-6.1-cleanup-implementation-plan.md`
**Verified plan-level assumptions section:** present

⚠️ 1 commit since plan-write time (SHA `123dd38d8`); cited file:line references re-checked under §1. (The single commit `82445c485` is the plan-add itself — it touches no code/doc the plan cites, so there is no real drift.)

## 1. Verified-plan-assumptions cross-check

Fresh read of each cited evidence reference. **All reconfirm.**

- **PA1** (testFixtures dir new) — holds. `services/jwt-auth-common/src/` contains only `main/`; no `testFixtures/`.
- **PA2** (`sdd-controller-practices.md` absent) — holds. File does not exist.
- **PA3** (4 consumer test files + 4 build.gradle exist) — holds. All 8 paths present.
- **PA4** (4 doc Modify targets exist) — holds. `sdd-reviewer-checklist.md`, `synthetic-payload-blind-spot.md`, `dev-env-setup.md`, `README.md` all present under `docs/process/`.
- **PA5** (`JwtService.COOKIE_NAME` public-static-final-String; fixture jjwt chain matches existing helpers) — holds. `JwtService.java:14`; the existing consumer helpers use the identical `Keys.hmacShaKeyFor(byte[])` + `Jwts.builder().subject().claim().issuedAt().expiration().signWith().compact()` (catalog `:114-120`, etc.) that the fixture reproduces.
- **PA6** (`validToken()` called only inside `authCookie()`; helpers private) — holds, with a refinement that strengthens the plan: in all 4 consumers, `TEST_SECRET` is referenced ONLY at its declaration + inside `validToken()` (the `Keys.hmacShaKeyFor(TEST_SECRET…)` line), and the `@DynamicPropertySource` secret line uses the **literal** string, not the constant. The plan's Task 3 Step 2 already accounts for this (delete `validToken`/`authCookie`/`TEST_SECRET`; repoint the secret line to `JwtTestFixtures.TEST_SECRET`) — so deleting the three members orphans nothing, and the secret-line edit is a clean literal→constant swap of an identical value.
- **PA7** (build/test commands valid) — holds. `services/gradlew` present; `useJUnitPlatform` `test` task in all 6 service build files; `:jwt-auth-common:testFixturesClasses` is the standard task the plugin contributes.
- **PA8** (RC-16 e2e spec + Playwright run) — holds. `e2e/tests/inventory-product-classifications.spec.ts` present; `.github/workflows/e2e-tests.yml:97` runs Playwright with `BASE_URL=http://localhost`.
- **PA9** (ordering; T1 doesn't affect T3 tests) — holds. Confirmed via A2 (no test depends on the 401-mask) + the consumer tests authenticate with valid tokens, which the ERROR-permit does not touch.
- **PA10** (test-fixtures gradle wiring valid) — holds. `services/build.gradle` `subprojects { apply plugin: 'java' }`; Gradle 8.5; `java-test-fixtures` is a built-in; `testImplementation(testFixtures(project(':jwt-auth-common')))` is the standard consumer form.
- **PA11** (RC-60 insertion anchor ×5; none imports DispatcherType) — holds. All 5 target configs have the `authorizeHttpRequests(a -> a … .requestMatchers(...).permitAll() … .anyRequest().authenticated())` shape; `grep -l DispatcherType` over the 5 returns none.
- **PA12** (doc sections net-new; README `## Files` list) — holds. No existing `## …` section titles for RC-57/58/59/60/62/63/65; `README.md:5 ## Files` is a bullet list that accepts a new entry.

## 2. Literal-wrongness findings

No literal-wrongness findings.

(Static pass — considered and dropped: the RC-60 code-block comment references the `sdd-reviewer-checklist.md` RC-60 section added in Task 4, but it's a Java comment with no compile/behavior impact and the referenced doc exists; the inventory invalid-jwt test's inline `new jakarta.servlet.http.Cookie(...)` is fully-qualified and survives the helper deletion untouched; the `@DynamicPropertySource` literal→`JwtTestFixtures.TEST_SECRET` swap preserves the exact value the service validates against.

Dynamic pass — considered and dropped: the ERROR-dispatch permit's runtime effect [unhandled exceptions surface their real status] is the spec's intended outcome and is already proven in production by inventory-service, so it introduces no runtime regression in the other 5 [document's in-controller `@ExceptionHandler`s still handle their cases in-dispatch before `/error`]; the normalized location token yields `locationId=null` via the filter, which location-service never reads and which the peer services already exercise; `java-test-fixtures` is consumed by 4 modules in one `./gradlew test` with a stateless static-method fixture [no shared-state race]; the done-gate `./gradlew build` assembles jwt-auth-common's main + testFixtures jars with `bootJar` disabled / `jar` enabled. None breaks the spec's stated outcome.)

## 3. Forced decisions

No forced decisions found.

(The plan's discretionary picks — static-import `authCookie` to keep call sites unchanged; dedup the `@DynamicPropertySource` literal into `JwtTestFixtures.TEST_SECRET`; one task for the 5 identical RC-60 edits; deferring the SDD execution cadence to SDD-time — are all author choices with a single correct/working outcome, not codebase-or-product-forced either/ors requiring user input.)

## 5. Recommendation

✅ **Approve as-is** — §1 has no failed assumptions; §2 and §3 are both empty. The plan is ready for `subagent-driven-development`.
